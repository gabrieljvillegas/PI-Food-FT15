import styled from "styled-components";

export const StyledDiv = styled.div`
  .recipe {
    color: white;
    background-color: black;
    max-width: 100%;
    height: 40rem;
    border-radius: 10px;
    padding: 1.5rem;
    p {
      font-size: 1.8rem;
    }
  }
`;
